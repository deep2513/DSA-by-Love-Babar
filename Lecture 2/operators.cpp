#include<bits/stdc++.h>
using namespace std;

int main(){
    
    int a = 2/5;
    cout << a << endl;

    float b = 2.0/5;
    cout << b << endl;

    int c = 2;
    int d = 3;

    // bool first = (a==b);
    // bool first = (a>b);
    // bool first = (a<b);
    // bool first = (a>=b);
    // bool first = (a<=b);
    bool first = (a!=b);
    cout << first << endl;

    int x = 0;
    cout << !x << endl;

    return 0;
}
